# TODO.md
## План работ проекта "Планка"

Источник roadmap: `CONTEXT.md`.

### Правило синхронизации с CHANGELOG
- `TODO.md` хранит план и статус задач.
- `CHANGELOG.md` фиксирует только выполненные изменения.
- При переводе задачи в `Done` сразу добавляем запись в `CHANGELOG.md` с ID задачи.
- Для инициативы переноса автоматики на Linux-host используется отдельный roadmap: `docs/pl-048-linux-hosted-flow-roadmap.md`.

### Статусы
- `Planned` - согласовано, но не начато.
- `In Progress` - в работе.
- `Done` - завершено и отражено в `CHANGELOG.md`.

## Инфраструктурный блок
- Цель блока: после merge в `main` автоматически обновлять hosting (`Ubuntu`, `/var/sites/planka`), чтобы проверять изменения с мобильных устройств.
- Статус блока: `PL-017`, `PL-019`, `PL-020` реализованы и переведены в эксплуатационный режим.
- Текущий шаг блока: `PL-022` - Telegram-сигналы на PR-ревью и финальные статусы post-merge workflow.

## Backlog

| ID | Задача | Scope | Приоритет | Статус | Критерий готовности |
| --- | --- | --- | --- | --- | --- |
| PL-001 | Подготовить презентацию проекта/продукта для ABA-куратора (первый артефакт продукта) | MVP | P0 | Done | Подготовлены артефакты: `docs/aba-curator-presentation-v1.md` и графичная v2 в `presentations/aba-v2/` (web-deck + mermaid для PDF) |
| PL-013 | Настроить GitHub Project dashboard и импортировать backlog задач с полями workflow | MVP | P0 | Done | Создан проект, добавлены поля (`Task ID`, `Priority`, `Scope`, `Flow`) и импортированы задачи `PL-001..PL-012` |
| PL-014 | Добавить переключение Light/Dark справа вверху (кнопка с луной/солнцем) | V1+ | P1 | Done | В прототипе есть явный toggle темы, состояние сохраняется и восстанавливается между перезапусками |
| PL-015 | Улучшить fallback портретной ориентации: без текста, с мнемоникой поворота | MVP | P0 | Done | При невозможности landscape-lock в portrait показывается только визуальная мнемоника поворота; текстовых предупреждений нет |
| PL-002 | Зафиксировать архитектурный выбор прототипа (`Native Android` vs `WebView + native shell`) через короткий spike | MVP | P0 | Done | Зафиксировано решение в `docs/pl-002-architecture-spike.md`, критерии в `docs/pl-002-validation-matrix.md`, выбран путь `WebView + Native Shell` с fallback в `Native Android` |
| PL-003 | Реализовать базовый экран 50/50 (верх: текст, низ: клавиатура), landscape-lock | MVP | P0 | Done | На запуске всегда открыт один и тот же экран, ориентация фиксирована |
| PL-004 | Реализовать детерминированную RU/EN клавиатуру без автокоррекции | MVP | P0 | Done | Ввод предсказуемый, раскладка стабильна, без скрытых жестов |
| PL-016 | Добавить кнопку Enter (перевод строки) в конец 3-го ряда клавиатуры | MVP | P0 | Done | Кнопка с символом Enter вставляет `\\n` в текст и стабильно доступна как последняя клавиша третьего ряда |
| PL-017 | Настроить автодеплой `main` на hosting (`Ubuntu`, `/var/sites/planka`) после merge PR | MVP | P0 | Done | Есть workflow автодеплоя по `push` в `main`, документированы секреты и есть ручной fallback-скрипт деплоя |
| PL-018 | Добавить верхний цифровой ряд над буквенными кнопками | MVP | P0 | Done | В клавиатуре есть отдельный верхний ряд `1..0`, доступный в обеих раскладках и не нарушающий стабильность существующих рядов |
| PL-019 | Автоматически переводить задачи `PL-xxx` в `Done` при merge PR в `main` | MVP | P0 | Done | Workflow извлекает `PL-xxx` из title/body merged PR и ставит соответствующим карточкам GitHub Project `Status=Done`, `Flow=Done` |
| PL-020 | Провести smoke-тест автозакрытия задач `PL-xxx` на merged PR в `main` | MVP | P0 | Done | После merge PR с ID `PL-019` и `PL-020` workflow `Project Auto Close Tasks` завершился успешно и обе карточки ушли в `Done` |
| PL-021 | Добавить Telegram push-уведомления о merge PR в `main` | MVP | P1 | Done | Поднят Telegram-канал уведомлений через GitHub Actions и secrets (`TG_BOT_TOKEN`, `TG_CHAT_ID`) |
| PL-022 | Добавить Telegram-сигналы на PR-ревью и финальные статусы post-merge workflow | MVP | P1 | In Progress | При создании/обновлении PR в `main` приходит сигнал на ревью; после merge приходят статусы `Deploy Main to Hosting` и `Project Auto Close Tasks` |
| PL-023 | Исследовать и внедрить app-auth path для Project v2 без личного PAT | MVP | P2 | Planned | Для user-owned Project v2 операции `Status/Flow` и чтение карточек выполняются не через `DAEMON_GH_PROJECT_TOKEN`, а через корректный app/user auth path; timeline Project больше не подписывается личным аккаунтом `justewg`; до подтвержденного smoke текущий hybrid mode остается штатным fallback |
| PL-024 | Убрать неоднозначность одноимённых personal Project v2 в toolkit smoke/create flow | MVP | P1 | Planned | Smoke/helper-операции не полагаются на `gh issue create --project <title>` и другие title-based привязки; добавление issue в Project и смена статусов идут только по каноническим `PROJECT_ID`/`PROJECT_NUMBER`, чтобы старые одноимённые personal projects не уводили карточки в чужую доску |
| PL-025 | Добавить мониторинг и cleanup зависших flow-helper процессов | MVP | P1 | Planned | Daemon/watchdog или отдельный housekeeping-проход раз в несколько тиков или раз в сутки обнаруживает аномальные цепочки helper-процессов (`gh_app_auth_token.sh` и аналоги), логирует инцидент и безопасно снимает зависшие хвосты без ручной чистки |
| PL-026 | Добавить wizard-генератор локальной flow-конфигурации и launchd-артефактов | MVP | P1 | Planned | После стабилизации нового `.flow`-layout есть bash wizard с пояснениями по каждому вопросу, который собирает `flow.env`, host-local runtime links (`logs/state/tmp`) и локальные `launchd`-plist/labels без хранения живых env/plist в git; wizard отдельно спрашивает, нужен ли Telegram-контур, и при отказе выставляет явный feature-flag для штатного skip без warn-спама; результат укладывается в единую структуру проекта и shared host-root |
| PL-045 | Зачистить stale reply/waiting runtime между соседними flow-задачами | MVP | P0 | Planned | После завершения одной Issue-задачи daemon/executor не переносят `daemon_user_reply*`, `daemon_waiting_*` и смежный waiting-context в следующую задачу; повторный smoke не требует ручной очистки runtime-state для выхода из ложного `WAIT_USER_REPLY` |
| PL-046 | Зафиксировать каноническую привязку `FLOW_HEAD_BRANCH` к активной task-ветке во всех flow-этапах | MVP | P0 | Planned | `executor`, `task_finalize`, `dev_commit_push`, daemon-launch env и branch-mismatch checks используют один и тот же актуальный head branch задачи и не ломаются из-за stale exported env или предыдущего smoke-прогона |
| PL-047 | Убрать ручной recovery-reset после executor fail в smoke-цикле | MVP | P1 | Planned | После падения executor daemon/watchdog штатно восстанавливают прогон до нового `executor_start` или корректного blocker/review-состояния без ручной очистки state-файлов и без локального доталкивания из терминала |
| PL-048 | Добавить Linux service-layer для daemon/watchdog с сохранением `launchd`-совместимости | MVP | P0 | Planned | `ai-flow` умеет ставить daemon/watchdog как `systemd`-services на Linux и как `launchd`-агенты на macOS; status/install/uninstall path симметричны, reboot-safe и не ломают текущий MacBook runtime |
| PL-049 | Ввести host runtime mode (`macos-local` / `linux-hosted`) в onboarding и `profile_init` | MVP | P0 | Planned | `profile_init init/install/orchestrate` и `onboarding_audit` знают про Linux-hosted режим, выбирают нужный service backend и не требуют ручной правки shell-логики при переносе на VPS |
| PL-050 | Добавить Linux-host bootstrap/preflight для executor, VPN и OpenAI contour | MVP | P0 | Done | Для `linux-docker-hosted` на VPS подтверждён канонический preflight: `codex` CLI, `CODEX_HOME`, `OPENAI_ENV_FILE`, safe VPN wrapper и OpenAI/VPN contour зелёные, а `profile_init preflight --profile planka` выдаёт `PREFLIGHT_READY=1` и корректно опирается на docker-hosted `status_snapshot` вместо host `systemd` unit checks |
| PL-051 | Зафиксировать single-runtime ownership, чтобы local и remote daemon не конкурировали за один profile | MVP | P0 | Planned | Для одного profile одновременно существует только один authoritative runtime; локальный checkout может оставаться interactive-only, не мешая server-side automation и не забирая те же задачи |
| PL-052 | Оформить `gh_app_auth` и `ops_bot` как штатный Linux-host runtime contour | MVP | P1 | In Progress | Audit PL-052 подтвердил Linux-host contour в shared toolkit: PM2 lifecycle/health для `gh_app_auth` и `ops_bot`, сохранение `/ops/status` + Telegram webhook + remote ingest, интеграцию с `profile_init`/Linux preflight и systemd runtime daemon/watchdog; при этом live recheck выявил незакрытые operational gaps (`PREFLIGHT_READY=0`, `ops_remote_status_push=404`, `ops_bot_post_smoke_check=incidents`), а quickstart/checklist в `.flow/shared` всё ещё частично launchd-centric |
| PL-053 | Перевести `planka` на reg.ru VPS как authoritative automation runtime и прогнать полный smoke | MVP | P0 | Planned | `planka` выполняет end-to-end flow на VPS без ручного доталкивания, а локальная интерактивная разработка в отдельном checkout не конфликтует с background automation |
| PL-054 | Документировать rollout, rollback и эксплуатационный режим Linux-hosted automation | MVP | P1 | Planned | Есть runbook переключения между MacBook и VPS, диагностики, rollback и правил ownership; новый инженер может повторить сценарий без reverse-engineering |
| PL-056 | Добавить `docker-hosted` deployment mode для ai-flow как server-appliance сценарий | MVP | P1 | In Progress | Есть wizard/installer, который генерирует `docker-compose.yml`, `Dockerfile`, env/secrets layout и authoritative workspace для profile; compose-контур `runtime`, `gh-app-auth`, `daemon`, `watchdog`, `ops-bot` поднимается через `docker compose`, использует host OpenAI/VPN path и даёт воспроизводимый runtime без ручной сборки host-native зависимостей; `status_snapshot` на VPS выходит в `HEALTHY`, а дальнейший update path сводится к `git pull` в `/.flow/shared` и `./up.sh` |
| PL-061 | Добавить optional remote-agent access contour для внешней AI-диагностики | MVP | P0 | Done | Для self-hosted ai-flow введён Remote Agent v2: отдельный user `aiflow`, deterministic `authorized_keys` forced-command, immutable gateway/helper вне repo, sanitized snapshots в `/var/lib/ai-flow/diagnostics/<profile>`, server-side `/etc/ai-flow/public` + `/etc/ai-flow/secrets`, loopback-only diagnostics surfaces, negative tests и documented kill switch/rollback без shell-доступа и без secret leakage |
| PL-062 | Усилить helper проверкой JSON-integrity snapshot перед выдачей probe-ответа | MVP | P1 | Done | Remote Agent v2 helper валидирует JSON snapshot перед выдачей `runtime_snapshot_v2`/metadata probes; при битом или невалидном JSON возвращает degraded-ответ с `snapshot_invalid_json=true` вместо повреждённого payload |
| PL-063 | Усилить diagnostics publisher проверкой JSON-integrity loopback status surfaces | MVP | P1 | Done | Publisher валидирует JSON от loopback status/health endpoints перед публикацией `runtime_snapshot.json`; при невалидном upstream JSON пишет degraded snapshot с `publisher_degraded=true` и флагами `invalid_*_json` |
| PL-064 | Phase 2: перевести runtime автоматику на отдельного service-user вместо operator account | MVP | P1 | Planned | Runtime automation (`docker-hosted` и/или host-native) работает под отдельным service-user вроде `ai-flow-runtime`, а не под операторским аккаунтом; blast radius runtime уменьшается, а operator user остаётся только для bootstrap/interactive maintenance |
| RA2-001 | Ввести immutable Remote Agent v2 bootstrap и server-side layout | MVP | P0 | Done | На VPS подтверждены immutable `gateway/helper/publisher`, layout `/etc/ai-flow/public`, `/etc/ai-flow/secrets`, `/var/lib/ai-flow/diagnostics/<profile>`, `aiflow` user, sudoers fragment и `systemd` timer без включения repo в root trust boundary |
| RA2-002 | Запустить deterministic diagnostics publisher и sanitized snapshot store | MVP | P0 | Done | Для профиля работает root-owned publisher через `systemd timer`, пишет sanitized snapshots в `/var/lib/ai-flow/diagnostics/<profile>`, соблюдает TTL=`2x interval`, не читает raw env/docker logs/repo-local env как diagnostic output |
| RA2-003 | Перевести SSH remote diagnostics на v2 helper/gateway без shell surface | MVP | P0 | Done | `aiflow` отдаёт `runtime_snapshot_v2` и `runtime_log_tail_v2`, arbitrary command режется через managed `authorized_keys`, а финальная модель не зависит от brittle `sshd_config.d` policy-layer |
| RA2-004 | Вынести server-side public/secrets config из legacy env-layout | MVP | P0 | Done | На VPS подтверждено runtime wiring через `/etc/ai-flow/public/*`, `/etc/ai-flow/secrets/*` и `/etc/ai-flow/secrets/platform/openai.env`; `compose_metadata.json` показывает `server_env_authority_wired=true`, `legacy_runtime_env_fallback=false`, а legacy `/var/sites/.ai-flow/config/*.env` остаются только migration/bootstrap fallback |
| RA2-005 | Закрыть внешний diagnostics ingress и оставить только loopback safe surfaces | MVP | P0 | Done | На VPS внешний nginx ingress для `/health`, `/ops/status`, `/ops/status.json`, `/ops/debug/*` уже отрезан; снаружи остаётся только `telegram/webhook`, а loopback `127.0.0.1:8790` продолжает обслуживать diagnostics surfaces |
| RA2-006 | Прогнать v2 negative tests, cutover и отключить legacy remote-agent v1 | MVP | P0 | Done | На VPS подтверждены negative scenarios: no shell, no arbitrary probe, no docker access, no secret read; legacy v1 server-side artifacts удалены, legacy v1 toolkit entrypoints отключены, а v2 contour считается каноническим optional probe-tier |
| PL-057 | Привести host-level nginx ingress ai-flow к v2 trust model | MVP | P0 | Planned | Host-level ingress на `aiflow.ewg40.ru` публикует только non-diagnostic surfaces (`/telegram/webhook` и при необходимости минимальный public liveness), а status/debug diagnostics остаются loopback-only и не проксируются наружу |
| PL-058 | Нормализовать platform paths и секреты под сущность `ai-flow` | MVP | P0 | Planned | В platform/project env и docker wiring не осталось legacy путей/имен вроде `planka-automation`; `OPENAI_ENV_FILE` и смежные host-level пути используют `ai-flow` naming |
| PL-059 | Прогнать server-runtime smoke на реальных `Todo` задачах | MVP | P0 | In Progress | Для `planka` уже подтверждены live-этапы `Todo -> In Progress`, daemon claim и `EXECUTOR_STARTED` на VPS/docker runtime без локального claim; `PL-050` закрыт, текущий runtime-state на VPS healthy (`PREFLIGHT_READY=1`, `IDLE_NO_TASKS`), а следующий full happy-path batch ждёт новую issue-backed карточку в `Todo` |
| PL-060 | Включить `env_audit` в эксплуатационный health-check и runbook | MVP | P1 | Planned | Для диагностики платформы штатно используются loopback `/health`, sanitized snapshots и `env_audit`; конфиг-дрифт отражается в diagnostics/runbook surface без секретов и без SSH-only reverse engineering |
| PL-034 | Narrative patch stage 1: Intro без бренда и без kicker | MVP | P1 | Planned | SECTION 0 содержит только фото + 2 строки (`Андрюша уже умеет говорить.` / `Просто не голосом.`), без `PLANKA` и без `ИНЖЕНЕРНЫЙ МАНИФЕСТ` |
| PL-035 | Narrative patch stage 2: Эволюция без wireframe в SECTION 1 | MVP | P1 | Planned | В SECTION 1 5-й шаг заменен на TV/Поиск или placeholder (`TODO_TV.png`), wireframe полностью убран из эволюции |
| PL-036 | Narrative patch stage 3: Связанные тезисы в SECTION 2 | MVP | P1 | Planned | SECTION 2 переписан на 5 связанных тезисов (PECS/пластилин/эрудит/TV/бумага), без абстрактных формулировок |
| PL-037 | Narrative patch stage 4: Мостик текста и порядок кадров в SECTION 3 | MVP | P1 | Planned | В SECTION 3 добавлены 2-3 строки мостика и зафиксирован порядок кадров: wireframe -> прототип UI |
| PL-038 | Narrative patch stage 5: Жест "СМОТРИ" в 4 кадра | MVP | P0 | Planned | SECTION 4 разложен на Письмо -> Намерение -> Показ -> Продолжил, добавлена финальная строка цикла (`Написал -> Показал -> Получил реакцию -> Продолжил.`) |
| PL-039 | Narrative patch stage 6: Корректные принципы в SECTION 5 | MVP | P1 | Planned | SECTION 5 содержит обновленный список принципов (без тезиса `без скрытых жестов`) и краткие формулировки |
| PL-040 | Narrative patch stage 7: Предметность SECTION 6 (фронт + задник) | MVP | P1 | Planned | SECTION 6 показывает фронт устройства и отдельный кадр задника/фактуры (реальный ассет или placeholder `TODO_BACK.png`) |
| PL-041 | Narrative patch stage 8: Разделение слоев в SECTION 7 | MVP | P1 | Planned | SECTION 7: кадр 1 `инструмент для него`, кадр 2 `dashboard взрослого слоя` (реальный ассет или placeholder `TODO_DASHBOARD.png`) |
| PL-042 | Narrative patch stage 9: Пространство роста в SECTION 8 | MVP | P1 | Planned | SECTION 8 разделяет центр/периферию слов и фиксирует периферийные фразы (`ХОЧУ ГУЛЯТЬ`, `БОЛИТ ЖИВОТ`, `Я НЕ ХОЧУ ЭТО`, `МНЕ НУЖЕН ПУЛЬТ`) |
| PL-043 | Narrative patch stage 10: Финал разделен на 9a и 9b | MVP | P1 | Planned | Финал разбит на две секции: 9a (посыл) и 9b (почти пустой экран с логотипом и сноской Планка) |
| PL-044 | Narrative patch stage 11: Print-режим по секциям | MVP | P0 | Planned | В `print.css` для всех narrative-секций подтвержден разрыв страницы (`page-break-after`) и корректная печать без слияния блоков |
| PL-005 | Добавить backspace с удержанием, безопасную очистку и переключение языка удержанием | MVP | P1 | Planned | Поведение клавиш стабильно и защищено от случайных нажатий |
| PL-006 | Добавить TTS-кнопку "Сказать" без модификации введенного текста | MVP | P1 | Planned | Текст проговаривается как введен, без автоисправлений |
| PL-007 | Добавить переворот только текстовой зоны на 180° по данным датчиков | MVP | P1 | Planned | При наклоне меняется только ориентация текста, клавиатура неизменна |
| PL-008 | Внедрить kiosk-ограничения: автозапуск, блокировка выхода/жестов/уведомлений | MVP | P0 | Planned | Невозможен случайный выход из режима использования |
| PL-009 | Реализовать родительский вход (секретная комбинация или PIN) | MVP | P1 | Planned | Вход в родительский режим доступен только по защищенному сценарию |
| PL-010 | Включить локальную надежность: автосохранение текста, восстановление после краша/перезагрузки, backup | MVP | P0 | Planned | Потеря текста при сбое отсутствует или минимизирована до последнего ввода |
| PL-011 | Настроить базовый CI для `development` (lint/test на PR) | V1+ | P2 | Planned | PR в `development` автоматически проверяются |
| PL-012 | Настроить release-процесс для `main` (подготовка к обновлению устройства) | V1+ | P2 | Planned | Есть воспроизводимый сценарий сборки и релиза |
| PL-065 | Усилить Android pseudo-kiosk режим на Lenovo TB-X606X до повторяемого эксплуатационного профиля | MVP | P0 | Planned | Тестовый shell на реальном Lenovo повторно скрывает system bars, устойчив к `focus loss` и возврату из launcher, а checklist hardening для `status bar` / `nav` / `home` оформлен и подтвержден серией ручных прогонов; реализация опирается на диагностические события `PL-066` и shell-доработки `PL-072` |
| PL-066 | Добавить диагностику и локальное логирование системных прорывов Android shell | MVP | P0 | Planned | В тестовом Android shell есть явный диагностический контур: события `window focus`, показ/скрытие system bars, попытки ухода в `home/recent`, `resume/pause/stop`, служебный экран/экспорт лога; по логу можно восстановить, где именно ломается pseudo-kiosk; результат нужен как база для `PL-065` и тестов `PL-073` |
| PL-067 | Выделить config-driven contract для UI-параметров клавиатуры и layout | MVP | P1 | Planned | Параметры UI (`text ratio`, `keyboard ratio`, rows, spacing, labels, feature flags) вынесены в явный versioned config-contract с fallback defaults; shell/WebView читают его детерминированно без пересборки APK; этот contract используется как база для `PL-069` и UX-итерации `PL-074` |
| PL-068 | Спроектировать HTTPS manifest update contract для Android shell | MVP | P0 | Done | Зафиксирован manifest contract для Android delivery: версии приложения и конфига, `minSupportedAppVersion`, `apk/config url`, `sha256`, `message`, failure policy, rollback semantics и manual check UX; схема не противоречит plan в `app/PLANKA_status_and_update_plan.md` и оформлена в `docs/pl-068-android-manifest-contract.md` как вход для `PL-069`, `PL-070`, `PL-071` |
| PL-069 | Реализовать config update без переустановки APK | MVP | P1 | Planned | Android shell умеет вручную проверить manifest, скачать config/resources по HTTPS, проверить `sha256`, применить новую конфигурацию локально и откатиться на предыдущую рабочую версию при ошибке; реализация использует contract из `PL-067` и manifest semantics из `PL-068` |
| PL-070 | Спроектировать безопасный APK update flow с проверкой hash/signature | MVP | P0 | Done | Зафиксирован канонический APK update flow в `docs/pl-070-android-apk-update-flow.md`: manual install only, `sha256` + package/version validation, trusted signing path через Android installer, distinction между optional/required update, install failure policy и publish invariants для `PL-071` |
| PL-071 | Поднять инфраструктуру публикации Android build-артефактов и manifest по HTTPS | MVP | P1 | Planned | Есть воспроизводимый publish path для `apk`, `config` и `manifest.json`: version bump, `sha256`, целевая HTTPS-директория, доступность с планшета и минимальный runbook выпуска; инфраструктура совместима с contract из `PL-068` и security assumptions из `PL-070` |
| PL-072 | Доработать тестовое Android-приложение до диагностического shell-build | MVP | P0 | Planned | Текущее `app/planka_quick_test_app` превращено из quick-test в рабочий диагностический shell: устойчивый immersive path, JS/native bridge для service actions, on-screen diagnostics entry, предсказуемый lifecycle и подготовка под ручные прогонки kiosk behavior; эта задача даёт реализационную базу для `PL-065` и `PL-066` |
| PL-073 | Сформировать и прогнать тестовый план на реальном Lenovo TB-X606X | MVP | P0 | Planned | Есть test matrix для реального планшета: `top swipe`, `bottom nav`, `home/recent`, `lock/unlock`, `rotation`, long idle, return-from-background, update checks; по каждому сценарию фиксируется expected/actual result и blocker severity; план использует diagnostics из `PL-066` и shell build из `PL-072` |
| PL-074 | Провести следующую UX-итерацию клавиатуры и пропорций экрана по данным реального планшета | MVP | P1 | Planned | На основе реального теста на Lenovo скорректированы пропорции текста/клавиатуры, размеры кнопок, интервалы и layout rules; изменения задаются через config-driven параметры без пересборки APK, а решение опирается на findings из `PL-073` и config-contract `PL-067` |
