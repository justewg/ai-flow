# PL-093 — Phase D / Dedup and execution lease

Цель:
- реализовать dedup key;
- запретить duplicate execution;
- ввести execution lease / heartbeat / stale policy.

Definition of done:
- identical expensive run не создаётся повторно;
- stale execution не порождает runaway restart chain.
