# PL-094 — Phase E / Budget guardrails and controlled rollout

Цель:
- добавить базовые budget caps и circuit breaker;
- ввести `PAUSED_BUDGET` / `EMERGENCY_STOP`;
- подготовить controlled rollout: `dry-run -> shadow -> single-task -> limited rollout`.

Definition of done:
- budget breach всегда останавливает automation;
- rollout идёт только через ограниченные режимы.
