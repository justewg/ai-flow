# PL-106 — Integration 12 / prepare accumulated integration changeset

Цель:
- упаковать `PL-096..PL-105` в PR-ready changeset, а не держать их только в локальном worktree;
- зафиксировать merge order и scope для `ai-flow` и `planka`;
- подготовить handoff к `PL-107`, где уже будет SAFE-mode обновление площадки.

Планируемый scope:
- PR-ready body files для `ai-flow` и `planka`;
- фиксация phase/result trail;
- локальные commits для submodule и root worktree;
- обновление worklog.

Definition of done:
- есть готовые PR body artifacts;
- `.flow/shared` changes закоммичены локально отдельным commit;
- root trail и gitlink закоммичены локально отдельным commit;
- следующая фаза может идти уже как SAFE rollout kit.
