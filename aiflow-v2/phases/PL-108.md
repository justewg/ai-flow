# PL-108 — Integration 14 / limited rollout promotion kit

Цель:
- подготовить reproducible promotion path из `SAFE` validation в `limited` rollout;
- не переводить систему в `auto`;
- сделать env update обратимым через backup.

Планируемый scope:
- env mutation script для rollout variables;
- checklist для operator step;
- локальная проверка скрипта на копии sample env.

Definition of done:
- есть `pl108_enable_limited_rollout.sh`;
- есть checklist;
- локальная проверка на копии env проходит и показывает ожидаемые keys.
