# PL-116 — Hard micro-profile guard after local canaries

Цель:
- включить следующий слой M3 после двух локальных canaries;
- перевести token budget breach из soft-observe в enforce-ready behaviour;
- добавить command guard для common discovery/file-reread shell paths внутри `micro` profile.

Планируемый scope:
- добавить deterministic `micro_command_guard` и `micro_prepare_guard_bin`;
- включить hard `FAILED_PROFILE_BREACH` path через `micro_profile_guard` и `executor_run`;
- прогнать guarded repair canary и отдельные smokes на blocked command и enforced budget breach.

Definition of done:
- command guard materialize-ит wrapper-bin и пишет `blocked_commands.jsonl`;
- direct blocked command smoke режется c `rc=126`;
- direct budget breach smoke возвращает `rc=42` и `FAILED_PROFILE_BREACH=1`;
- guarded repair canary всё ещё проходит в рамках `2 calls / <15k tokens`.
