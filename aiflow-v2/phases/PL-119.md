# PL-119 — Micro classifier target-file extraction fix after live canary

Цель:
- устранить false-negative в `micro` classifier, который был вскрыт live run `PL-119`;
- перестать считать `targetFiles` по backtick-путям из секции `Вне scope`;
- сохранить детектирование реальных target files из title/context/`Что сделать`.

Планируемый scope:
- скорректировать `micro_profile_extract_target_files` в `.flow/shared/scripts/micro_profile_lib.sh`;
- ограничить extraction телом issue до секций до `Проверки` / `Вне scope`;
- игнорировать command-like backtick fragments с пробелами.

Definition of done:
- реплика текста `PL-119` даёт `targetFiles=["narrative/index.html"]`;
- classifier verdict для такого текста становится `micro`, а не `standard` по `target_count_4`;
- existing local canary/runbook logic не ломается.
