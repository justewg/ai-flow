# PL-122 — Micro parser indentation tolerance for copied issue bodies

Цель:
- устранить ещё один live blocker для `micro` classifier;
- сделать parser tolerant к leading spaces, которые добавляет интерфейс `codex-cli` при копировании текста issue в терминал;
- не допускать раздувания `targetFiles` и потери checks из-за отступов перед `Проверки:` / `Вне scope:` и bullet lines.

Планируемый scope:
- обновить `.flow/shared/scripts/micro_profile_lib.sh`;
- нормализовать matching по trimmed section headers;
- сделать line-aware extraction для target files и checks без хрупких regex по backtick-командам.

Definition of done:
- реплика issue body с двумя ведущими пробелами перед каждой строкой даёт корректные `targetFiles` и `check` commands;
- `targetFiles` для live-like `PL-121` body схлопываются до `["narrative/index.html"]`;
- `Проверки:` и `Вне scope:` распознаются независимо от indentation.
