# PL-123 — Live micro follow-up after first profile breach

Цель:
- исправить узкие orchestration-баги, вскрытые первым живым `micro` run `PL-123`;
- убрать ложный `failed_check` для `git diff -- <path>`;
- жёстко останавливать `micro` flow сразу после первого `profile_breach`;
- ввести deterministic no-op short-circuit для already-satisfied tasks без второго LLM call.

Планируемый scope:
- расширить whitelist verification commands в `micro_profile_lib.sh`;
- обновить `executor_run.sh`:
  - re-check budget breach после каждого LLM call;
  - не ходить в repair branch, если budget уже breached;
  - при пустом canonical diff после успешных checks завершать run как `noop_already_satisfied` без `micro_finalize`.

Definition of done:
- `git diff -- narrative/index.html` проходит whitelist в `micro` profile;
- при `profileBreach=true` после первого call repair-call не запускается;
- no-op micro task завершает run без второго call и без finalize chatter.
