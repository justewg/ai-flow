# PL-127 — Intake artifacts and source capture

Цель:
- ввести canonical intake artifacts до старта execution;
- материализовать `Source Definition` как отдельный слой без переписывания исходной постановки.

Планируемый scope:
- добавить artifact paths:
  - `source_definition.json`
  - `standardized_task_spec.json`
  - `intake_profile.json`
- добавить scripts:
  - `task_capture_source_definition.sh`
  - `task_standardize_spec.sh`
  - `task_interpret.sh`
- сохранить raw title/body/reply как audit source.

Definition of done:
- intake artifacts materialize-ятся в execution dir task worktree;
- source definition живёт отдельно от execution profile;
- локальный smoke может собрать intake artifacts без запуска executor.
