# PL-129 — Intake-driven profile decision

Цель:
- сделать profile decision производной от intake layer, а не отдельной classifier-веткой на сыром issue text.

Планируемый scope:
- превратить `micro_task_classifier.sh` в thin wrapper поверх `task_interpret`;
- materialize `execution_profile.json` из intake artifacts;
- сохранить compatibility для `executor_start` и existing runtime logs.

Definition of done:
- `micro_task_classifier` больше не классифицирует raw issue напрямую;
- `execution_profile.json` ссылается на `source_definition`, `standardized_task_spec` и `intake_profile`;
- local smoke показывает `EXECUTION_PROFILE` из intake layer.
