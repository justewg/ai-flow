# PL-094 Result Log

Статус: `done`

## Цель фазы
- добавить базовые budget caps и circuit breaker;
- ввести `PAUSED_BUDGET` / `EMERGENCY_STOP`;
- подготовить controlled rollout: `dry-run -> shadow -> single-task -> limited rollout`.

## Что делаем
- добавляем budget/rollout policy layer поверх `runtime-v2`;
- считаем task-level execution usage;
- вводим explicit automation gate для controlled rollout.

## Что реализовано
- Добавлен policy layer: `.flow/shared/runtime-v2/src/budget_policy.js`.
- Реализованы:
  - `evaluateTaskBudget()`
  - `enforceTaskBudget()`
  - `evaluateRolloutGate()`
  - `evaluateAutomationGate()`
- Добавлены task-level budget caps:
  - `maxExecutionsPerTask`
  - `maxTokenUsagePerTask`
  - `maxEstimatedCostPerTask`
- Budget breach теперь переводит task в:
  - `budgetState=emergency_stop` по умолчанию
  - `lockState=frozen`
- Подготовлен controlled rollout scaffold:
  - `dry_run`
  - `shadow`
  - `single_task`
  - `limited`
  - `auto`
- Store/adapters расширены списком executions по задаче для budget evaluation.

## Изменённые файлы
- `.flow/shared/runtime-v2/src/budget_policy.js`
- `.flow/shared/runtime-v2/src/store.js`
- `.flow/shared/runtime-v2/src/memory_adapter.js`
- `.flow/shared/runtime-v2/src/mongo_adapter.js`
- `.flow/shared/runtime-v2/src/index.js`
- `.flow/shared/runtime-v2/test/store.test.js`
- `.flow/shared/runtime-v2/README.md`

## Проверки
- `node --test test/*.test.js` в `.flow/shared/runtime-v2`: `OK`
- локальный smoke на `evaluateRolloutGate()`:
  - `dry_run` блокирует expensive automation
  - `single_task` пропускает только allowlisted task

## Ограничения Phase E
- Budget policy пока task-level; global/day-level spend caps ещё не добавлены.
- Circuit breaker пока выражен через task stop-state, без отдельного project/global control record.
- Runtime shell layer пока не читает этот policy напрямую; это следующий интеграционный шаг.
