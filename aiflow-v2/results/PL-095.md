# PL-095 Result Log

Статус: `done`

## Цель фазы
- подключить `runtime-v2` как отдельный shadow contour;
- materialize-ить v2 state из legacy runtime без переключения automation;
- держать все новые integration entrypoints с явным префиксом `runtime_v2`.

## Что делаем
- добавляем persistent `file` adapter;
- вводим `runtime_v2_shadow_sync`, `runtime_v2_snapshot`, `runtime_v2_clear`;
- строим отдельный `runtime_v2/store` рядом с legacy state-dir.

## Что реализовано
- Добавлен persistent `file adapter` для `runtime-v2`.
- Добавлен legacy shadow projection:
  - `collectLegacyShadowSnapshot()`
  - `inferTaskStateFromLegacy()`
  - `buildSnapshotHash()`
- Добавлены отдельные `v2`-prefixed wrapper scripts:
  - `runtime_v2_shadow_sync.sh`
  - `runtime_v2_snapshot.sh`
  - `runtime_v2_clear.sh`
- В `run.sh` добавлены команды:
  - `runtime_v2_shadow_sync`
  - `runtime_v2_snapshot`
  - `runtime_v2_clear`
- `runtime_v2` shadow state теперь живёт отдельно от legacy runtime в:
  - `<state-dir>/runtime_v2/store`
- Shadow sync materialize-ит:
  - `Task`
  - `TaskState`
  - optional `Execution`
  - `legacy.shadow_synced` events

## Изменённые файлы
- `.flow/shared/runtime-v2/src/file_adapter.js`
- `.flow/shared/runtime-v2/src/legacy_shadow.js`
- `.flow/shared/runtime-v2/bin/runtime_v2_shadow_sync.js`
- `.flow/shared/runtime-v2/bin/runtime_v2_snapshot.js`
- `.flow/shared/runtime-v2/src/index.js`
- `.flow/shared/runtime-v2/test/file_adapter.test.js`
- `.flow/shared/scripts/runtime_v2_shadow_sync.sh`
- `.flow/shared/scripts/runtime_v2_snapshot.sh`
- `.flow/shared/scripts/runtime_v2_clear.sh`
- `.flow/shared/scripts/run.sh`
- `.flow/shared/scripts/README.md`

## Проверки
- `node --test test/*.test.js` в `.flow/shared/runtime-v2`: `OK`
- shell validation новых `runtime_v2_*` wrappers: `OK`
- local smoke с отдельным `CODEX_STATE_DIR`:
  - `runtime_v2_clear`: `OK`
  - `runtime_v2_shadow_sync`: `OK`
  - `runtime_v2_snapshot PL-095`: `OK`

## Ограничения PL-095
- Это пока shadow bridge: legacy runtime не читает verdicts из `runtime-v2`.
- Mongo пока не участвует; persistent shadow contour сейчас файловый и изолированный.
- Следующий шаг — policy-first gating или shell-to-v2 bridge чтения, не full switch.
