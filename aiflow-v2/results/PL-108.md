# PL-108 Result Log

Статус: `done`

## Цель фазы
- подготовить reproducible promotion path из `SAFE` validation в `limited` rollout;
- не включать `auto`;
- сделать env mutation обратимой через backup.

## Что реализовано
- Добавлен promotion script:
  - `aiflow-v2/rollout/pl108_enable_limited_rollout.sh`
- Добавлен checklist:
  - `aiflow-v2/rollout/PL-108-limited-rollout-checklist.md`
- Script:
  - создаёт backup env file
  - выставляет `FLOW_V2_ROLLOUT_MODE=limited`
  - выставляет `FLOW_V2_ALLOWED_TASK_IDS=<csv>`
  - фиксирует базовые budget guardrails

## Проверки
- `bash -n aiflow-v2/rollout/pl108_enable_limited_rollout.sh`
- local smoke на копии env:
  - `bash aiflow-v2/rollout/pl108_enable_limited_rollout.sh --env-file <tmp> --allow-task PL-105 --allow-task PL-106`
  - result:
    - backup file created
    - expected `FLOW_V2_*` keys written

## Notes
- Это promotion kit, не actual target enable.
- Реальное включение limited rollout остаётся operator step после успешного `PL-107` target validation.
