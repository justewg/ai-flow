# PL-110 result

Status: done locally

Changes:
- added claim-grace handling to `.flow/shared/scripts/runtime_v2_reconcile_primary_context.sh`
- reconcile now preserves freshly claimed `daemon_active_*` for a short window while `runtime-v2` has not yet materialized `phase=executing`

Verification:
- `bash -n .flow/shared/scripts/runtime_v2_reconcile_primary_context.sh`
- local smoke with empty primary `active` + fresh `daemon_last_claim_epoch.txt` now prints:
  - `RUNTIME_V2_PRIMARY_ACTIVE_CLAIM_GRACE=1`
  - preserved `daemon_active_task.txt=PL-109`

Reason:
- limited rollout canary `PL-109` showed a successful claim followed by an orphaned `In Progress` task with no executor start.
- root cause: primary reconcile cleared the legacy active context before the next daemon tick reached `executor_tick`.
