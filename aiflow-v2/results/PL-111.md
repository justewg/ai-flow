# PL-111 result

Status: done locally

Changes:
- rewrote `.flow/shared/scripts/executor_build_prompt.sh` to generate prompt files through `printf`, not an expanding heredoc
- added tolerant payload parsing to `.flow/shared/runtime-v2/bin/runtime_v2_apply_event.js`

Verification:
- `bash -n .flow/shared/scripts/executor_build_prompt.sh`
- `bash -n .flow/shared/scripts/runtime_v2_apply_event.sh`
- local prompt smoke with backticks in issue body now emits a clean prompt file and no shell errors
- local `runtime_v2_apply_event.js` smoke with `{\"reason\":\"provider quota exceeded\"}TRAILING_JUNK` now applies the event and reports `payloadWarning`

Notes:
- this fixes flow-layer crashes around the executor bootstrap path
- provider quota exhaustion itself remains an external condition and still needs available Codex/OpenAI quota on the target runtime
