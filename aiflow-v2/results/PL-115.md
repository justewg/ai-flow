# PL-115 result

Status: done locally

Changes:
- extended `.flow/shared/scripts/micro_local_canary.sh` with `repair-smoke` mode
- synthetic fake `codex` now supports an intentionally incomplete first change, so deterministic checks force the second `repair` call

Verification:
- local repair-path synthetic canary:
  - `./.flow/shared/scripts/run.sh micro_local_canary PL-115 115 repair-smoke`
- resulting artifacts:
  - `llm_calls.jsonl` -> two calls (`implementation`, `repair`)
  - `execution_budget.json` -> `callCount=2`, `totalTokens=6346`, `profileBreach=false`
  - `check_results.json` -> both checks passed after repair

Observed telemetry:
- `implementation` call -> `2890` total tokens
- `repair` call -> `3456` total tokens
- total -> `6346` tokens

Reason:
- after the happy-path `PL-114`, this stage confirms that the optional second call works as a bounded repair path instead of collapsing back into open-ended multi-turn orchestration.
