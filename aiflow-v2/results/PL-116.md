# PL-116 result

Status: done locally

Changes:
- added `.flow/shared/scripts/micro_command_guard.sh` and `.flow/shared/scripts/micro_prepare_guard_bin.sh`
- updated `.flow/shared/scripts/executor_run.sh` to prepare guard-bin for `micro` calls and to propagate hard `FAILED_PROFILE_BREACH`
- updated `.flow/shared/scripts/llm_call_telemetry.sh` to mark healthy budget state as `ok`
- updated `.flow/shared/scripts/executor_start.sh` so micro budget enforcement defaults to enabled after local canaries
- exposed `micro_prepare_guard_bin` in `.flow/shared/scripts/run.sh`

Verification:
- shell syntax:
  - `bash -n .flow/shared/scripts/micro_command_guard.sh`
  - `bash -n .flow/shared/scripts/micro_prepare_guard_bin.sh`
  - `bash -n .flow/shared/scripts/executor_run.sh`
- direct block smoke:
  - `PATH="$GUARD_DIR:$PATH" cat README.md` -> blocked with `rc=126`
- direct profile breach smoke:
  - crafted `execution_budget.json` with `callCount=3`, `totalTokens=16100`, `enforceProfileBreach=true`
  - `./.flow/shared/scripts/run.sh micro_profile_guard PL-BREACH 1` -> `FAILED_PROFILE_BREACH=1`, `rc=42`
- guarded repair canary:
  - `./.flow/shared/scripts/run.sh micro_local_canary PL-116 116 repair-smoke`
  - `execution_budget.json` -> `callCount=2`, `totalTokens=6346`, `status=ok`, `profileBreach=false`
  - `check_results.json` -> both checks passed
  - `blocked_commands.jsonl` stayed empty during the successful guarded canary
- `node --test .flow/shared/runtime-v2/test/*.test.js`

Reason:
- after `PL-114` and `PL-115` proved the bounded micro-path, this stage makes the budget envelope and no-discovery shell guard enforceable instead of advisory-only.
