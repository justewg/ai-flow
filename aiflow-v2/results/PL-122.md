# PL-122 result

Status: done locally

Changes:
- updated `.flow/shared/scripts/micro_profile_lib.sh`
- `micro_profile_extract_target_files` now:
  - matches `Проверки:` / `Вне scope:` after trimming leading spaces
  - skips negative-scope bullets like `Не менять...` / `не трогать...`
  - extracts backtick paths line-by-line instead of scanning the whole issue blob
- `micro_profile_extract_check_commands` now:
  - trims leading spaces before matching section headers
  - parses bullet items without regex that depends on raw backtick line shape

Verification:
- shell syntax:
  - `bash -n .flow/shared/scripts/micro_profile_lib.sh`
- local replicated issue body with two leading spaces on every line:
  - `targetFiles=["narrative/index.html"]`
  - extracted checks:
    - `rg -n "planka-hero__bg-print" narrative/index.html`
    - `rg -n "aria-hidden|alt=" narrative/index.html`
    - `git diff -- narrative/index.html`

Reason:
- the previous live run for `PL-121` proved that the earlier classifier fix was still incomplete: copied issue text kept the leading spaces from the terminal UI, so section headers were not recognized and `targetFiles` were inflated again.
