# PL-124 result

Status: done locally

Changes:
- updated `.flow/shared/scripts/micro_profile_lib.sh`
  - added `micro_profile_extract_context_anchors`
  - added anchor-based excerpt range extraction and merged range rendering
  - `micro_profile_file_context_json` now emits compact `@@ lines x-y @@` excerpts instead of fixed top-of-file slices
- updated `.flow/shared/scripts/context_builder.sh`
  - pass full issue text into `micro_profile_file_context_json` so file context can be selected around live task anchors

Verification:
- shell syntax:
  - `bash -n .flow/shared/scripts/micro_profile_lib.sh`
  - `bash -n .flow/shared/scripts/context_builder.sh`
- local smoke on replicated indented `PL-124` issue body:
  - `micro_profile_extract_context_anchors` returns focused anchors such as `hero` and `aria-label`
  - `micro_profile_file_context_json /private/var/sites/PLANKA narrative/index.html ... | jq -r '.excerpt'`
    now returns the `planka-hero` excerpt instead of an arbitrary top-of-file dump

Reason:
- `549.log` proved the live `micro` path works end-to-end, but also exposed that `context_builder` still sent the wrong fragment of a large file, so the model could not safely edit the intended HTML node within budget.
