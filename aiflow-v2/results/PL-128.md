# PL-128 result

Status: done locally

Changes:
- `task_standardize_spec.sh` now builds `standardized_task_spec.json` with:
  - `profileDecision`
  - `decisionReason`
  - `interpretedIntent`
  - `candidateTargetFiles`
  - `expectedChange`
  - `checks`
  - `confidence`
  - `notes`
  - `rationale`
  - `repositoryContext`
- repository-aware scoping now falls back from not-yet-materialized task worktree to repo root during intake-time target discovery

Verification:
- local smoke on replicated `PL-125` task body with leading spaces:
  - `candidateTargetFiles=["narrative/index.html"]`
  - `checks` restored correctly
  - `profileDecision="micro"`
  - `confidence.label="high"`

Reason:
- intake layer needed a normalized task contract before profile selection and prompt building.
