# PL-130 result

Status: done locally

Changes:
- updated `.flow/shared/scripts/context_builder.sh`
  - reads `source_definition.json`, `standardized_task_spec.json`, `intake_profile.json`
  - builds `context_cache.json` from normalized spec
  - micro prompt now uses `Standardized task spec` instead of raw issue body
- updated `.flow/shared/scripts/executor_build_prompt.sh`
  - standard profile now starts from normalized spec
  - source definition is kept as audit-only block
- updated `.flow/shared/scripts/executor_start.sh`
  - logs source/spec/intake artifact paths
  - blocks `human_needed | blocked` before executor launch
- `context_builder` and `task_standardize_spec` now have repo-root fallback when task worktree is not yet materialized

Verification:
- `bash -n` on updated `context_builder.sh`, `executor_build_prompt.sh`, `executor_start.sh`
- local micro smoke with replicated `PL-125` task body:
  - prompt starts from `Standardized task spec`
  - `File context` is built from normalized target files
- local standard smoke:
  - standard prompt starts from `Normalized task spec`
  - raw source appears only in `Source Definition (audit only)` block
- local ambiguous smoke:
  - `micro_task_classifier` returns `human_needed`

Limits:
- full `executor_start` block-path could not be exercised end-to-end locally because local environment currently sits in `SAFE` containment mode before executor launch.

Reason:
- interpretation and execution are now separated: executor receives normalized task contract first and no longer treats free-form issue body as primary source of intent.
